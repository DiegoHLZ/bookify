spring:
  application:
    name: bookify-backend

  datasource:
    url: ${BOOKIFY_DB_URL}
    username: ${BOOKIFY_DB_USERNAME}
    password: ${BOOKIFY_DB_PASSWORD}

  flyway:
    enabled: true
    validate-migration-naming: true
    locations:
      - classpath:db/migration
      - classpath:db/vendor/{vendor}

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

server:
  port: ${BOOKIFY_SERVER_PORT:8080}
  shutdown: graceful

jwt:
  secret: ${BOOKIFY_JWT_SECRET}
  expiration: ${BOOKIFY_JWT_EXPIRATION_MS:86400000}

bookify:
  invitations:
    expiration-hours: ${BOOKIFY_INVITATION_EXPIRATION_HOURS:72}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never
